<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.luffy.test.android">

    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />

    <application>
        <activity android:name=".ui.settings.SettingsActivity"></activity>
        <activity android:name=".ui.swipingCard.SwipingCardActivity" />
        <activity android:name=".ui.provider.TestProviderActivity" />
        <activity android:name=".ui.provider.ContentProviderActivity" />
        <activity android:name=".ui.bankCard.BankCardResultActivity" />
        <activity android:name=".ui.bankCard.BankCardDetailActivity" />

        <service
            android:name=".service.DaemonIntentService"
            android:exported="false" />
        <service
            android:name=".service.DaemonService"
            android:enabled="true"
            android:exported="true" />

        <provider
            android:name=".provider.TestProvider"
            android:authorities="com.luffy.test.provider.feature"
            android:exported="true" />
    </application>

</manifest>